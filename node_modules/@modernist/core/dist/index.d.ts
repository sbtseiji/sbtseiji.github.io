import { Command, Config } from "@modernist/types";
declare const modernist: ({ name, args }: Command, config: Config, rootDir: string) => Promise<import("@modernist/types").ManifestEntry[]>;
export default modernist;
